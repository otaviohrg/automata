- name: Install Docker and docker-compose
  become: true
  community.general.pacman:
    name:
      - docker
      - docker-compose
      - docker-buildx
    state: present

- name: Enable and start Docker
  become: true
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Add user to docker group
  become: true
  ansible.builtin.user:
    name: "{{ ansible_facts['user_id'] }}"
    groups: docker
    append: true

- name: Install nvidia-container-toolkit
  become: true
  kewlfft.aur.aur:
    name: nvidia-container-toolkit
    state: present
  register: nct_install

- name: Restart Docker after nvidia-container-toolkit
  become: true
  ansible.builtin.systemd:
    name: docker
    state: restarted
  when: nct_install.changed

- name: Verify GPU accessible inside container
  ansible.builtin.command: >
    docker run --rm --gpus all
    nvidia/cuda:12.1.0-base-ubuntu22.04 nvidia-smi
  register: gpu_check
  changed_when: false
  failed_when: gpu_check.rc != 0
