- name: Install Tailscale
  become: yes
  community.general.pacman:
    name: tailscale
    state: present

- name: Enable and start tailscaled
  become: yes
  systemd:
    name: tailscaled
    enabled: yes
    state: started

- name: Check if already authenticated
  command: tailscale status
  register: ts_status
  changed_when: false
  failed_when: false

- name: Authenticate Tailscale
  become: yes
  command: "tailscale up --authkey={{ tailscale_auth_key }} --advertise-tags=tag:workstation"
  when: "'Logged out' in ts_status.stdout or ts_status.rc != 0"
