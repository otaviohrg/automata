name: CI - container images
on:
  push:
    paths:
      - 'shared/images/**'
      - '.github/workflows/ci-containers.yml'
  pull_request:
    paths:
      - 'shared/images/**'
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build automata-base
        uses: docker/build-push-action@v5
        with:
          context: shared/images/automata-base
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ghcr.io/${{ github.repository_owner }}/automata-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build automata-ml
        uses: docker/build-push-action@v5
        with:
          context: shared/images/automata-ml
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ghcr.io/${{ github.repository_owner }}/automata-ml:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
